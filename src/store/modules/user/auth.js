import axios from 'axios'

const state ={
    requireAuth:true,
};

const getters ={};

const mutations = {
  
};
const actions = {
   
};

export default {
    namespaced:true,
    state,
    getters,
    mutations,
    actions
}