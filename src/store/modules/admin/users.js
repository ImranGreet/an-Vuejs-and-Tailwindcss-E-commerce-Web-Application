import axios from 'axios'

const state ={
    requireAuth:false,
};

const getters ={};

const mutations = {
  
};
const actions = {
   
};

export default {
    namespaced:true,
    state,
    getters,
    mutations,
    actions
}